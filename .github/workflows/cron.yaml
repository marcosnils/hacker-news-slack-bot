name: daily-cron
on:
  schedule:
    - cron: '* * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call our API route
        run: |
          curl --X POST 'https://discord-hacker-news-bot.vercel.app/api/cron'

